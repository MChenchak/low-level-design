1. Реализация АТД в Java - абстрактный класс. При необходимости указвыается тип-параметр - Generic.

Сравнение с эталоном:

2. Формирование списка методов (с сигнатурами).
Все методы делю на 4 категории: конструкторы, команды(требуют пост и/или предусловие), запросы(требуют предусловия), доп. зарпосы

Сравнение с эталоном:

    Конструкторы: ok

    Команды:
    head(): ok
    tail(): ok
    right(): в эталоне пред/пост условия определены точнее. Но в целом кажется мне удалось правильно их сформулировать
    putRight(): курсор содержит ссылку на существующий узел == список не пуст? кажется да
    putLeft(): аналогично putRight()
    remove(): куда смещается курсор, если элемент был единственным в списке?
    clear(): ok
    addTail(): ok
    removeAll(): забыл написать постусловие!!!
    replace(T val): ok
    find(T val): в эталоне нет предусловия. видимо подразумевается, что вызывать этот метод есть смысл только
    для непустого списка. в таком случае курсор точно должен содержать ссылку

    Запросы: ok

    Вспомогательные методы:

3. Машина состояний: снова не смог определить машину состояний хоть немного близкую к эталонной.
    в след раз для каждого метода надо задать вопрос: требуется ли получать результат выполнения операции. если да,
    то нужно определить эти статусы и методы для их получения.


Сделал ошибку объявив в абстрактом классе узлы для хранения текущего, головного и хвостового узлов. Тем самым я
опустился на более низкий уровень абстракции, чего не нужжно было делать.
В реализация АТД может в качестве типа узлов может быть использован тип, отличный от Node.