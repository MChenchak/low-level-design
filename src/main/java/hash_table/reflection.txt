1. Конструктор определен верно. Но я почему-то решил сделать его реализацию, совершенно забыв,
    что описываю АТД.
    Наличие в АТД свойства для хранения максимального размера таблицы, вроде бы не ошибка. или ошибка?
    По крайней мере оно точно не обязательно, т.к может быть определено в классе-реализации.

    Не указал постусловие для конструктора по невнимательности.

2. Для метода put я посчитал важным указать предусловие о допустимом количестве колизий, веедь в теории все новые
    элементы могут попасть в один и тот же слот. и тогда вставка.поиск элементов не будут работать за О(1),
    а будут работать за О(n).

3. Метод remove отсутствует в моеем решении, т.к этот метод не был упомянут в задании по структурам данных

4. понимаю, что для метода get наличие преедусловия совсем не обязательно, т.к и на пустой таблице этот метод должен отрабатывать корректно.
    Все равно, что в непустой таблице нет искомого значения

5. Метод расчета хеш-значения добавлен, т.к этот метод был в задании по АТД. хотя, конечно же этот метод в АТД лишний.
